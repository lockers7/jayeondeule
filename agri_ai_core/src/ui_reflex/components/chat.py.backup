# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Reflex 채팅 컴포넌트
# 채팅 메시지 표시 및 히스토리 관리
# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
import reflex as rx
from ..state import ChatState
from ..styles import CHAT_BUBBLE_USER, CHAT_BUBBLE_ASSISTANT, CHAT_CONTAINER_STYLE, FILE_ATTACHMENT_STYLE


# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 파일 첨부 표시 컴포넌트
# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
def file_attachment(file: dict) -> rx.Component:
    """파일 첨부 표시"""
    return rx.box(
        rx.hstack(
            rx.text("📎", margin_right="4px"),
            rx.text(
                f"{file['name']} ({file.get('size', 0) / 1024:.1f} KB)",
                font_size="12px",
                color="#1976D2",
            ),
            spacing="4px",
        ),
        **FILE_ATTACHMENT_STYLE,
    )


# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 사용자 메시지 버블
# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
def user_message_bubble(message: dict) -> rx.Component:
    """사용자 메시지 버블"""
    return rx.box(
        rx.box(
            rx.markdown(message["content"]),
            rx.cond(
                message.get("files", []),
                rx.box(
                    rx.foreach(message.get("files", []), file_attachment),
                ),
                rx.box(),
            ),
            **CHAT_BUBBLE_USER,
        ),
        display="flex",
        justify_content="flex-end",
        width="100%",
    )


# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 어시스턴트 메시지 버블
# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
def assistant_message_bubble(message: dict) -> rx.Component:
    """어시스턴트 메시지 버블"""
    return rx.box(
        rx.box(
            rx.markdown(message["content"]),
            **CHAT_BUBBLE_ASSISTANT,
        ),
        display="flex",
        justify_content="flex-start",
        width="100%",
    )


# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 채팅 버블 컴포넌트 (조건부 렌더링)
# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
def chat_bubble(message: dict) -> rx.Component:
    """
    채팅 메시지 버블 렌더링 (조건부)

    Args:
        message: 메시지 딕셔너리 (role, content, files, timestamp)

    Returns:
        rx.Component: 채팅 버블 컴포넌트
    """
    return rx.cond(
        message["role"] == "user",
        user_message_bubble(message),
        assistant_message_bubble(message),
    )


# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 채팅 히스토리 컴포넌트
# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
def chat_history() -> rx.Component:
    """
    전체 채팅 히스토리 렌더링

    Returns:
        rx.Component: 채팅 히스토리 컨테이너
    """
    return rx.box(
        rx.cond(
            ChatState.messages,
            rx.foreach(
                ChatState.messages,
                chat_bubble
            ),
            rx.box(
                rx.text(
                    "💬 대화를 시작해보세요!",
                    color="#6c757d",
                    font_size="14px",
                    text_align="center",
                ),
                padding="40px",
            )
        ),
        **CHAT_CONTAINER_STYLE,
    )
