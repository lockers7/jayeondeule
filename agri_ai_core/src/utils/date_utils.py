# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 날짜 유틸리티 모듈
# 날짜 파싱, 포맷 변환, 날짜 범위 계산 등
# 날짜/시간 관련 공통 유틸리티 함수를 제공합니다.
# --->
# parse_datetime: 다양한 형식의 날짜/시간 문자열을 datetime 객체로 변환
# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
from datetime import datetime


# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 다양한 형식의 날짜/시간 문자열을 datetime 객체로 변환
# --->
# 다양한 형식의 날짜/시간 문자열을 datetime 객체로 변환
# Args:
# dt_str: 날짜 시간 문자열
# default_formats: 시도할 형식 목록
# Returns:
# datetime: 변환된 datetime 객체 또는 None
# ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
def parse_datetime(dt_str, default_formats=None):
    if not dt_str:
        return None

    if isinstance(dt_str, datetime):
        return dt_str

    formats = default_formats or [
        "%Y-%m-%d %H:%M:%S",
        "%Y-%m-%d"
    ]

    for fmt in formats:
        try:
            return datetime.strptime(dt_str, fmt)
        except ValueError:
            continue

    return None


