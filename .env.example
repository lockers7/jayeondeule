# =========================================================================
# AgriAI Core 환경 변수 설정
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env
# =========================================================================

# ------------------------------------------------------------
# Reflex UI 설정
# ------------------------------------------------------------
# REFLEX_ENV=prod  → 프로덕션 모드 (단일 포트 3000)
# REFLEX_ENV=dev   → 개발 모드 (프론트 3000, 백엔드 8001)
REFLEX_ENV=prod
REFLEX_PUBLIC_HOST=your-domain.example.com
REFLEX_PUBLIC_PORT=3000

# ------------------------------------------------------------
# 데이터베이스 설정
# ------------------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_username
DB_PASSWORD=your_password

# ------------------------------------------------------------
# ChromaDB 설정
# ------------------------------------------------------------
CHROMA_HOST=localhost
CHROMA_PORT=8000

# ------------------------------------------------------------
# 로그 설정
# ------------------------------------------------------------
LOG_PATH=/workspace/jayeondeule/logs
LOG_LEVEL=INFO

# ------------------------------------------------------------
# Ollama LLM 설정
# ------------------------------------------------------------
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2:latest

# ------------------------------------------------------------
# 기타 설정
# ------------------------------------------------------------
# Python 경로 (필요시)
# PYTHONPATH=/workspace/jayeondeule
